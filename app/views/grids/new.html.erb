<h1>New grid</h1>

<% form_for(@grid) do |f| %>
  <%= f.error_messages %>

  <p>
  	<%=collection_select(:grid, :campaign_id, @campaigns, :id, :title, {:prompt => true}) %>
  </p>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', characters_path %>